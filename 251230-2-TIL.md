#TIL
Today I Learned

##Template

###TIL

```markdown
#251230

##오늘 한 것

###pre-commit
- commit 전에 수행해야 되는 것들. repo를 만들때마다 설정하기

- pip list -> pip install pre-commit -> pre-commit --v(버전확인) -> [pre-commit sample-config -> pre-commit-config.yaml] -> pre-commit install(프로젝트 당 한번만)

- rm.git/hooks/pre-commit : 프리커밋이 귀찮을때

- git commit -m "" -n : 일시적으로 프리커밋을 사용하지 않을때


###hooks 추가 방법(https://pre-commit.com/hooks.html)
- vi .pre-commit-config.yaml

- repo: https://github.com/psf/black
    rev: stable
    hooks:
    - id: black
      language_version: python3.14.2
-   repo: https://github.com/PyCQA/flake8
    rev: 7.0.0
    hooks:
    - id: flake8

###branch
- git branch : 분기점을 생성하여 독립적으로 코드를 변경할수 있도록 도와줌 ( 독립된 공>간)

- git switch {branch} : 해당 브랜치로 전환

- git commit -m "text" == git commit (Enter) -> text

- git merge {branch} : 브랜치를 끌어오다

- git branch -D "파일이름" : 해당 브랜치 삭제

- merge commit : 첫째줄에 이름이 있어 "-m"사용 지양하기

- git push -u origin {branch} : 처음 올리는 브랜치

###깃허브 메인에 가면 있는데 git bash가면 없는거 옮길때??? (급하게 적은거라 무슨소린지
 모르겠음)
- git fetch origin main -> git merge FETCH_HEAD

###github Issue & Pull requests 작성

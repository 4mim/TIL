# How to Start git

## Contents

1. What is git?

## What is git?

git은 Linus Trovalds가 만든 분산형 버전관리 시스템

### Pros

### Commands

```shell
$ git clone {erpo addr}
$ cd {repo name}
$ git status

$ git add {filename}
$ git commit
$ git push origin main
```## What is git?

git은 Linus Trovalds가 만든 분산형 버전관리 시스템

### Pros

### Commands

```shell
$ git clone {erpo addr}
$ cd {repo name}
$ git status

$ git add {filename}
$ git commit
$ git push 

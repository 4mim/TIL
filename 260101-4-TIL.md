# TIL
Today I Learned

## Template

### TIL

```markdown
# 260101

## 오늘 한 것

### git복습

- mkdir : 폴더 생성

- mv {파일이름} {폴더이름} : 해당 파일을 해당 폴더로 이동

- mv {파일이름.*} {폴더이름} : 해당 파일이름을 가진 모든 확장자를 해당 폴더로 이동

- cp {파일이름} ./ : 해당 경로에 파일 복사

- cp {파일이름} ./ {바꿀 파일이름} : 해당 경로에 바뀐 파일이름으로 파일 복사

- cp {파일이름} {폴더이름} : 해당 폴더로 파일 복사

- mv {파일이름1} {파일이름2} : 파일이름1을 파일이름2로 변경

- rmdir : 폴더 삭제

- rm -r : 파일이 들어있는 폴더까지 모두 삭제

### fizzbuzz를 사용하여 복습

- 기본 세팅 : new repo -> git clone -> .gitignore -> README.md 편집 -> add, commit

- touch 파일생성 -> vi 파일편집(코드) -> py 코드실행확인 -> git status,add,commit -> git push origin main

## 특이사항

- git commit을 vim에서 작성 후 저장 시 git status부터 commit 까지의 기록이 보이지 않음 (동작은 됨)

- 파일 이름 변경 후 git push origin main 까지 했음에도 로컬에서는 바뀐 파일이 보이지 않으나 github에는 남아있는걸 확인

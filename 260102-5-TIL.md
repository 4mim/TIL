# 260102

## 오늘 한 것 -> 온라인 강의

### 웹 서버와 웹 어플리케이션 서버

- 정적인 자원 : 언제/어디서/누가 봐도 변하지 않는 정보 (HTML, 이미지, 동영상 등)
- 동적인 자원 : 언제/어디서/누가 보는지에 따라 변할 수 있는 정보 (실시간으로 기온이 변하는 사이트, 회원 사이트의 회원 이름 등) -> 여기서 주로 사용되는것이 데이터베이스

정적인 자원 을 응답하는 웹 서버
동적인 자원을 생성해 응답하는 웹 어플리케이션 서버

웹서비스 : 웹 서버 + 웹 어플리케이션 서버 같이 사용 -> 과도한 부하 방지 / 여러 웹 어플리케이션 서버 연동 용이

### HTTP의 특성

- 기본 특성
   - 요청-응답 기반 클라이언트- 서버 구조 프로토콜
     (서버 간에도 HTTP 메세지를 주고 받을 수 있음)
   - 미디어-독립적 프로토콜
     (어떤 형태의 데이터도 HTTP 메세지로 보낼 수 있음)
   - 비연결성 프로토콜
     (HTTP 1.0 / 1.1 / 2.0은 TCP 기반 // TCP는 연결성 프로토콜 // 
   - 스테이트리스 프로토콜
      (서버는 클라이언트의 상태를 기억하지 않음)
      (왜 스테이트리스 프로토콜? -> 스테이트풀이라면 클라이언트는 한 서버에 종속될 우려가 있음 / 여러개 요청을 보내야 할 경우 한 서버에만 요청해야함)
   - 지속 연결 프로토콜 (킵 얼라이브 / Keep-Alive)
       (연결 할 때마다 3-way handshake -> 혼잡, 시간 지연
       (하나의 연결을 이용해 여러개의 HTTP 요청/응답 주고받기)

### HTTP 메세지 개관

- 메세지 구조
   - start-line
   - header-field CRLF
   - CRLF
   - message-body <- 미디어 독립적 (다양한 데이터 형식이 들어감)

- HTTP 요청 헤더 - start-line
   -> HTTP 메서드 (공백) 요청 대상 (공백) HTTP 버전 (개행)

- HTTP 응답 헤더 - start-line
   -> HTTP 버전 (공백) 응답 코드 (공백) 이유 문구 (개행)

### [실습] HTTP 요청-응답 메세지 직접 확인하기

- curl -X <요청 매서드> URL (디폴트:GET)
-> 매서드는 GET이나 POST, PUT, DELETE같은 그거
-> -X를 빼고 보내면 그냥 GET을 보내는것과 같다.

- curl -I (URL) -> 응답 메세지 받기 (헤더만 출력)
- curl -i (URL) -> 응답 메세지 받기 (헤더와 본문 같이 출력)
- curl -v (URL) -> 응답 메세지 받기 (요청 과정까지 같이 받기)
- curl -o {File.txt} -X GET (URL) -> 요청 결과를 특정파일에 저장 -> {File.txt}에 메세지를 저장할건데, 요청 메서드는 GET으로  (URL)에 대해서 요청을 보낸다.
- curl -d {전송할 데이터}\ - 바디 데이터를 실어서 보낼수 있음
   - -X POST URL)
- curl -H {전송할 데이터}\ - 전송할 헤더 추가(전송할 헤더 포함해서 보내기) >
Content-Type:{타입명} -X POST (URL)
ex) curl -X PUT -d (전송할 데이터)

### 데이터베이스

- 기본 키와 외래 키
- 데이터 입력_INSERT
- 데이터 조회_SELECT
- 데이터 수정_UPDATE
- 데이터 삭제_DELETE

### 효율적 쿼리

- 인덱스
- 뷰
- JOIN

### gitflow 복습

- 이틀차 실습파일을 이용해 merge 기능으로 충돌을 일으킨 후 파일 수정 및 업로드

- main브랜치와 다른 브랜치에서 파일을 각각 작업 후 main으로 넘어와 merge 후 수정

## 특이사항

- merge 후 수정하고 난 뒤 git commit인지 git merge commit 인지 아직 헷갈린다.
- 우선 git commit으로 넘어가졌다. (이부분은 강사님께 여쭤보기)
- gitflow 작업한것만 잔디가 찍히는거 같다... 

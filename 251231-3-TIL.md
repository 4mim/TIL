# TIL
Today I Learned

## Template

### TIL

```markdown
# 251230

## 오늘 한 것

### gitflow 복습

- github와 bash를 병행하며 충돌사항 수정하기

### 파일 수정

- [README.md] Issue 생성 -> Edit 클릭 후 직접 수정 -> bash에서 강제로 충돌 -> branch 생성 후 switch -> vi README.md 직접수정 -> add, commit, push -> 자동머지가 불가능하지만 일단 진행 -> resolve conficts를 눌러 직접 해결(문서)
※ [코드파일] git bash로 돌아가 fetch origin main, merge -> vim 열어서 수정 > add, commit, push > fetch origin main -> merge FETCH_HEAD

### Stash

- git stash -> 작업중인 변경사항 잠시 미뤄두기

- git stash list - 미뤄둔 작업사항 확인

- git stash pop - 꺼내오기

- git stash drop - 변경사항 삭제

※ branch의 생명주기가 길어도 문제가 됨

### Undo

- git restore (파일이름) -> 가장 마지막에 찍은 commit으로부터 커밋되지 않은 최신 커밋으로 돌아갈 때

-git checkout -- (파일이름) -> switch + restore 기능

### Unstaging

- git restore --staged (파일이름) -> git add까지 하고 commit 전에 수정하고 싶을때 

### Edit commit message

- git commit --amend -> 직전 commit message 수정하기 [commit 끝나고 바로 사용]

- Reset commit - 없던일로 만들기 (가급적 사용하지 않기) / 돌아옴, 저장소 날라감

- Revert commit - 잘못을 인정하고 특정시점으로 이동
-> git revert --no-commit HEAD~{nums of commit}.. -> git revert --no-commit HEAD~3.. -> 3번의 작업을 뒤로 돌아가기

## 실습 (팀프로젝트)

### 팀장

- new organization 만들기
- 팀원 초대(팀원은 멤버로만)
- new repo 만들기
- Issue 탬플릿 제작
- git clone / .gitignore
- 공용으로 사용할 파일 제작
- 팀원 이슈 확인
- 코드리뷰 (변경요청,피드백 등)
- 승인 후 merge
- 프로젝트, 위키, 이슈 틈틈히 작업하기

### 팀원

- Issue 생성
- Fork -> git clone 후 작업
- git remote add upstream (팀주소) -> git push -u origin fb (두번째 push 부터 -u사용x)
- compare & pull request
- 이슈연결 resolve #(해시태그)
※ 팀의 코드를 받기 위해
  - git switch main -> git remote -v -> git fetch upstream main -> git merge FETCH_HEAD -> git push origin main

### 기타

- Fork : origin / Team Repository : upstream
- commit 시 prefix 작성하는게 아직 헷갈림
- 사도세자
